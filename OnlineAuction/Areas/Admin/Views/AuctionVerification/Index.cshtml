@model AuctionVerificationListViewModel

@{
    ViewData["Title"] = "Verify Auctions";
    ViewData["AdminPage"] = "verify-auctions";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.css" />
<link rel="stylesheet" href="~/admin/css/daterangepicker-admin.css" asp-append-version="true" />

<div class="mx-auto max-w-(--breakpoint-2xl) p-4 md:p-6">
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <div>
            <h2 class="text-title-md font-bold text-gray-800 dark:text-white/90">Verify Auctions</h2>
            <p class="text-theme-sm text-gray-500 dark:text-gray-400">Review seller listings before they go live.</p>
        </div>
    </div>

    @if (TempData["SuccessMessage"] is not null)
    {
        <div class="mb-4 rounded-lg border border-success-200 bg-success-50 px-4 py-3 text-theme-sm text-success-700">
            @TempData["SuccessMessage"]
        </div>
    }

    @if (TempData["ErrorMessage"] is not null)
    {
        <div class="mb-4 rounded-lg border border-error-200 bg-error-50 px-4 py-3 text-theme-sm text-error-700">
            @TempData["ErrorMessage"]
        </div>
    }

    <div class="mb-4 rounded-xl border border-gray-200 bg-white p-4 dark:border-gray-800 dark:bg-white/[0.03]">
        <form id="verificationFilterForm" method="get" class="flex flex-wrap items-end gap-3 2xl:flex-nowrap">
            <input type="hidden" name="Page" value="@(Model.Filter.Page <= 0 ? 1 : Model.Filter.Page)" />

            <div class="min-w-[220px] flex-1">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Search</label>
                <input id="verificationSearchInput"
                       name="Search"
                       value="@Model.Filter.Search"
                       placeholder="Product, seller, or category"
                       autocomplete="off"
                       class="h-10 w-full rounded-lg border border-gray-300 px-3 text-theme-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white/90" />
            </div>

            <div class="w-[180px]">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Category</label>
                <select name="CategoryId" class="admin-filter-control h-10 w-full rounded-lg border border-gray-300 px-3 text-theme-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white/90">
                    <option value="">All</option>
                    @foreach (var category in Model.CategoryOptions)
                    {
                        <option value="@category.Value" selected="@category.Selected">@category.Text</option>
                    }
                </select>
            </div>

            <div class="w-[150px]">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Listing type</label>
                <select name="ListingType" class="admin-filter-control h-10 w-full rounded-lg border border-gray-300 px-3 text-theme-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white/90">
                    <option value="">All</option>
                    <option value="auction" selected="@(Model.Filter.ListingType == "auction")">Auction</option>
                    <option value="buynow" selected="@(Model.Filter.ListingType == "buynow")">Buy Now</option>
                </select>
            </div>

            <div class="w-[300px]">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Date range</label>
                @{
                    ViewData["DateRangeId"] = "verificationDateRangePicker";
                    ViewData["DateRangeHiddenId"] = "verificationDateRangeValue";
                    ViewData["DateRangeName"] = "DateRange";
                    ViewData["DateRangeValue"] = Model.Filter.DateRange;
                    ViewData["DateRangePlaceholder"] = "Select date range";
                }
                @await Html.PartialAsync("_DateRangeTrigger")
            </div>
        </form>
    </div>

    <div id="verificationListResults" class="transition-opacity duration-200">
        <partial name="_AuctionVerificationList" model="Model" />
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/jquery/latest/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/momentjs/latest/moment.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.min.js"></script>
    <script src="~/admin/js/admin-list-filter.js" asp-append-version="true"></script>
    <script>
        AdminListFilter.init({
            formId: 'verificationFilterForm',
            resultsId: 'verificationListResults',
            searchInputId: 'verificationSearchInput',
            listUrl: '@Url.Action("Index", "AuctionVerification", new { area = "Admin" })',
            setupDateRange: function (submitCallback) {
                AdminListFilter.setupDateRangePicker('#verificationDateRangePicker', submitCallback);
            }
        });
    </script>
}
