@model BuyNowListViewModel

@{
    ViewData["Title"] = "Buy Now Management";
    ViewData["AdminPage"] = "buy-now";
}

<link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.css" />
<link rel="stylesheet" href="~/admin/css/daterangepicker-admin.css" asp-append-version="true" />

<div class="mx-auto max-w-(--breakpoint-2xl) p-4 md:p-6">
    <div class="mb-6 flex flex-col gap-3 sm:flex-row sm:items-center sm:justify-between">
        <div>
            <h2 class="text-title-md font-bold text-gray-800 dark:text-white/90">Buy Now Management</h2>
            <p class="text-theme-sm text-gray-500 dark:text-gray-400">Manage Buy Now listings currently on sale, pending review, or sold.</p>
        </div>

        @if (Model.CanManage)
        {
            <a asp-area="Admin"
               asp-controller="BuyNow"
               asp-action="Create"
               class="admin-icon-action admin-icon-action--primary admin-icon-action--lg"
               title="Create Buy Now Listing"
               aria-label="Create Buy Now Listing">
                <partial name="_AdminIcon" model='"plus"' />
            </a>
        }
    </div>

    @if (TempData["SuccessMessage"] is not null)
    {
        <div class="mb-4 rounded-lg border border-success-200 bg-success-50 px-4 py-3 text-theme-sm text-success-700">
            @TempData["SuccessMessage"]
        </div>
    }

    @if (TempData["ErrorMessage"] is not null)
    {
        <div class="mb-4 rounded-lg border border-error-200 bg-error-50 px-4 py-3 text-theme-sm text-error-700">
            @TempData["ErrorMessage"]
        </div>
    }

    <div class="mb-4 rounded-xl border border-gray-200 bg-white p-4 dark:border-gray-800 dark:bg-white/[0.03]">
        <form id="buyNowFilterForm" method="get" class="flex flex-wrap items-end gap-3 2xl:flex-nowrap">
            <input type="hidden" name="Page" value="@(Model.Filter.Page <= 0 ? 1 : Model.Filter.Page)" />

            <div class="min-w-[220px] flex-1">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Search</label>
                <input id="buyNowSearchInput"
                       name="Search"
                       value="@Model.Filter.Search"
                       placeholder="Product, category, or seller"
                       autocomplete="off"
                       class="h-10 w-full rounded-lg border border-gray-300 px-3 text-theme-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white/90" />
            </div>

            <div class="w-[180px]">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Category</label>
                <select name="CategoryId" class="admin-filter-control h-10 w-full rounded-lg border border-gray-300 px-3 text-theme-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white/90">
                    <option value="">All</option>
                    @foreach (var category in Model.CategoryOptions)
                    {
                        <option value="@category.Value" selected="@category.Selected">@category.Text</option>
                    }
                </select>
            </div>

            <div class="w-[200px]">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Seller</label>
                <select name="SellerId" class="admin-filter-control h-10 w-full rounded-lg border border-gray-300 px-3 text-theme-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white/90">
                    <option value="">All</option>
                    @foreach (var seller in Model.SellerOptions)
                    {
                        <option value="@seller.Value" selected="@seller.Selected">@seller.Text</option>
                    }
                </select>
            </div>

            <div class="w-[300px]">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Date range</label>
                @{
                    ViewData["DateRangeId"] = "buyNowDateRangePicker";
                    ViewData["DateRangeHiddenId"] = "buyNowDateRangeValue";
                    ViewData["DateRangeName"] = "DateRange";
                    ViewData["DateRangeValue"] = Model.Filter.DateRange;
                    ViewData["DateRangePlaceholder"] = "Select date range";
                }
                @await Html.PartialAsync("_DateRangeTrigger")
            </div>

            <div class="w-[170px]">
                <label class="mb-1 block text-theme-xs font-medium text-gray-500">Sort</label>
                <select name="SortOrder" class="admin-filter-control h-10 w-full rounded-lg border border-gray-300 px-3 text-theme-sm dark:border-gray-700 dark:bg-gray-900 dark:text-white/90">
                    <option value="" selected="@(string.IsNullOrWhiteSpace(Model.Filter.SortOrder))">Newest</option>
                    <option value="date_asc" selected="@(Model.Filter.SortOrder == "date_asc")">Oldest</option>
                    <option value="price_desc" selected="@(Model.Filter.SortOrder == "price_desc")">Price high-low</option>
                    <option value="price_asc" selected="@(Model.Filter.SortOrder == "price_asc")">Price low-high</option>
                    <option value="name_asc" selected="@(Model.Filter.SortOrder == "name_asc")">Name A-Z</option>
                    <option value="name_desc" selected="@(Model.Filter.SortOrder == "name_desc")">Name Z-A</option>
                </select>
            </div>
        </form>
    </div>

    <div id="buyNowListResults" class="transition-opacity duration-200">
        <partial name="_BuyNowList" model="Model" />
    </div>
</div>

@section Scripts {
    <script src="https://cdn.jsdelivr.net/jquery/latest/jquery.min.js"></script>
    <script src="https://cdn.jsdelivr.net/momentjs/latest/moment.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/daterangepicker/daterangepicker.min.js"></script>
    <script src="~/admin/js/admin-list-filter.js" asp-append-version="true"></script>
    <script>
        function bindBuyNowBulkActions(container) {
            AdminListFilter.bindBulkDelete(container, {
                formId: 'buyNowBulkDeleteForm',
                selectAllId: 'selectAllBuyNow',
                checkboxClass: 'buynow-checkbox',
                emptyMessage: 'Please select at least one Buy Now listing.',
                confirmMessage: 'Are you sure you want to delete selected Buy Now listings?'
            });
        }

        AdminListFilter.init({
            formId: 'buyNowFilterForm',
            resultsId: 'buyNowListResults',
            searchInputId: 'buyNowSearchInput',
            listUrl: '@Url.Action("Index", "BuyNow", new { area = "Admin" })',
            setupDateRange: function (submitCallback) {
                AdminListFilter.setupDateRangePicker('#buyNowDateRangePicker', submitCallback);
            },
            onUpdated: bindBuyNowBulkActions
        });
    </script>
}
