@model OnlineAuction.Models.CreateBuyNowViewModel

<div class="sell-section overflow-hidden rounded-xl border border-slate-200 bg-white shadow-sm">
  <div class="border-b border-slate-100 px-6 py-5">
    <h2 class="flex items-center gap-3 text-base font-semibold text-slate-900">
      <span class="flex h-7 w-7 shrink-0 items-center justify-center rounded-full bg-blue-700 text-xs font-bold text-white">4</span>
      @Localizer["SellBuyNow_Section3_Title"]
    </h2>
    <p class="mt-1 pl-10 text-sm text-slate-500">@Localizer["SellBuyNow_Section3_Desc"]</p>
  </div>

  <div class="p-6">
    <div class="max-w-sm">
      <label for="price" class="mb-1.5 block text-sm font-medium text-slate-700">@Localizer["SellBuyNow_Price"] <span class="text-red-500">*</span></label>
      <div class="relative">
        <span class="pointer-events-none absolute left-3 top-1/2 -translate-y-1/2 text-sm text-slate-400">$</span>
        <input type="number" id="price" name="Price" value="@(Model.Price > 0 ? Model.Price : "")" step="0.01" min="0" placeholder="500"
               class="field-input w-full rounded-lg border border-slate-200 bg-white py-2.5 pl-7 pr-4 text-sm outline-none transition focus:border-blue-500 focus:ring-2 focus:ring-blue-100"/>
      </div>
      <p class="mt-1 text-xs text-slate-500">@Localizer["SellBuyNow_PriceHint"]</p>
      <p class="field-error mt-1 hidden text-xs text-red-600" data-for="price"></p>
    </div>
  </div>
</div>
