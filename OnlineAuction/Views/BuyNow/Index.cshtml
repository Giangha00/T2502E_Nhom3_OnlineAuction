@model OnlineAuction.Models.AuctionViewModel
@{
    ViewData["Title"] = "Buy Now";
    var totalCount = Model.Auctions.Count;
}

<div class="rarecard-page bg-white">
  <section class="border-b border-stone-200 px-4 py-10 sm:px-8 lg:px-12 lg:py-12">
    <div class="flex flex-col gap-6 lg:flex-row lg:items-end lg:justify-between">
      <div>
        <h1 class="text-3xl font-bold text-slate-900 sm:text-4xl lg:text-5xl">Buy Now</h1>
        <p class="mt-3 max-w-2xl text-sm leading-relaxed text-slate-600 sm:text-base">
          Shop authenticated, high-grade collectibles at fixed prices — ready to add to cart and checkout instantly.
        </p>
      </div>
      <div class="flex flex-wrap items-center gap-3">
        <span class="inline-flex items-center gap-2 rounded-full bg-blue-50 px-4 py-2 text-xs font-semibold text-blue-800">
          <span class="h-2 w-2 rounded-full bg-blue-600"></span>
          Available now
        </span>
        <button type="button" class="rounded-full border border-stone-300 px-4 py-2 text-xs font-semibold text-slate-700">My Cart (0)</button>
        <button type="button" class="rounded-full border border-stone-300 px-4 py-2 text-xs font-semibold text-slate-700">Watchlist (0)</button>
      </div>
    </div>
  </section>

  <div class="flex w-full flex-col lg:flex-row">
    <aside class="rarecard-sidebar shrink-0 border-b border-stone-200 bg-slate-50 px-4 py-6 sm:px-8 lg:w-72 lg:border-b-0 lg:border-r lg:px-6 xl:w-80">
      <form id="auctionFilterForm" class="space-y-6">
        <div>
          <h3 class="text-xs font-bold uppercase tracking-wider text-slate-900">Category</h3>
          <div class="mt-3 space-y-2">
            @foreach (var category in Model.Categories)
            {
              <label class="flex cursor-pointer items-center gap-2 text-sm text-slate-700">
                <input type="checkbox" name="category" value="@category.Name" class="filter-category rounded border-stone-300 text-blue-700 focus:ring-blue-600"/>
                @category.Name
              </label>
            }
          </div>
        </div>

        <div>
          <h3 class="text-xs font-bold uppercase tracking-wider text-slate-900">Condition</h3>
          <div class="mt-3 space-y-2">
            @foreach (var cond in new[] { "Graded", "New", "Used", "Not Specified" })
            {
              <label class="flex cursor-pointer items-center gap-2 text-sm text-slate-700">
                <input type="checkbox" name="condition" value="@cond" class="filter-condition rounded border-stone-300 text-blue-700 focus:ring-blue-600"/>
                @cond
              </label>
            }
          </div>
        </div>

        <div>
          <h3 class="text-xs font-bold uppercase tracking-wider text-slate-900">Year Manufactured</h3>
          <div class="mt-3 space-y-2">
            @foreach (var (label, min, max) in new (string, int, int)[] {
              ("Pre-1990", 0, 1989),
              ("1990–1999", 1990, 1999),
              ("2000–2009", 2000, 2009),
              ("2010–2019", 2010, 2019),
              ("2020+", 2020, 9999)
            })
            {
              <label class="flex cursor-pointer items-center gap-2 text-sm text-slate-700">
                <input type="checkbox" name="year" value="@min-@max" data-min="@min" data-max="@max" class="filter-year rounded border-stone-300 text-blue-700 focus:ring-blue-600"/>
                @label
              </label>
            }
          </div>
        </div>

        <div>
          <h3 class="text-xs font-bold uppercase tracking-wider text-slate-900">Price Range</h3>
          <div class="mt-3 space-y-2">
            @foreach (var (label, min, max) in new (string, int, int)[] {
              ("Under $1,000", 0, 999),
              ("$1,000 – $5,000", 1000, 5000),
              ("$5,000 – $25,000", 5000, 25000),
              ("$25,000 – $50,000", 25000, 50000),
              ("$50,000+", 50000, 99999999)
            })
            {
              <label class="flex cursor-pointer items-center gap-2 text-sm text-slate-700">
                <input type="checkbox" name="price" value="@min-@max" data-min="@min" data-max="@max" class="filter-price rounded border-stone-300 text-blue-700 focus:ring-blue-600"/>
                @label
              </label>
            }
          </div>
        </div>

        <button type="button" id="clearFilters" class="w-full rounded-full border border-stone-300 py-2 text-xs font-semibold uppercase tracking-wider text-slate-600 hover:bg-white">
          Clear filters
        </button>
      </form>
    </aside>

    <section class="min-w-0 flex-1 px-4 py-6 sm:px-8 lg:px-10 lg:py-8">
      <div class="mb-6 flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between">
        <p id="resultCount" class="text-sm text-slate-500">Showing @totalCount items</p>
        <div class="flex items-center gap-3">
          <label for="auctionSearch" class="sr-only">Search</label>
          <input id="auctionSearch" type="search" placeholder="Search rare items..."
                 class="w-full rounded-full border border-stone-200 bg-stone-50 px-4 py-2.5 text-sm outline-none focus:border-blue-600 sm:max-w-xs"/>
          <select id="auctionSort" class="rounded-full border border-stone-200 bg-white px-3 py-2 text-sm outline-none focus:border-blue-600">
            <option value="default">Featured</option>
            <option value="price-asc">Price: low to high</option>
            <option value="price-desc">Price: high to low</option>
            <option value="name">Name A–Z</option>
          </select>
        </div>
      </div>

      <div id="auctionGrid" class="rarecard-auction-grid">
        @foreach (var item in Model.Auctions)
        {
          <partial name="Partials/_TradingCardAuctionCard"
                   model="@(new AuctionCardViewModel { Item = item, EnableFiltering = true, Variant = "grid", CardMode = "buynow" })"/>
        }
      </div>

      <div id="emptyState" class="hidden rounded-2xl border border-dashed border-stone-300 py-20 text-center">
        <p class="text-xl font-bold text-slate-800">No cards found</p>
        <p class="mt-2 text-sm text-slate-500">Try adjusting your filters or search.</p>
      </div>

      <div id="pagination" class="mt-12 flex flex-col items-center gap-4 border-t border-stone-200 pt-10 sm:flex-row sm:justify-center sm:gap-8">
        <button type="button" id="prevPage" class="text-sm font-semibold text-slate-600 hover:text-slate-900 disabled:opacity-30" disabled>← Previous</button>
        <div id="pageNumbers" class="flex flex-wrap items-center justify-center gap-1"></div>
        <button type="button" id="nextPage" class="text-sm font-semibold text-slate-600 hover:text-slate-900 disabled:opacity-30">Next →</button>
      </div>
    </section>
  </div>
</div>

@section Scripts {
  <script>
    (function () {
      var PAGE_SIZE = 12;
      var grid = document.getElementById('auctionGrid');
      var emptyState = document.getElementById('emptyState');
      var pagination = document.getElementById('pagination');
      var resultCount = document.getElementById('resultCount');
      var pageNumbers = document.getElementById('pageNumbers');
      var prevPageBtn = document.getElementById('prevPage');
      var nextPageBtn = document.getElementById('nextPage');
      var searchInput = document.getElementById('auctionSearch');
      var sortSelect = document.getElementById('auctionSort');
      var clearBtn = document.getElementById('clearFilters');
      var cards = Array.from(document.querySelectorAll('.auction-card'));

      var currentPage = 1;
      var filteredCards = [];

      function getCheckedValues(selector) {
        return Array.from(document.querySelectorAll(selector + ':checked')).map(function (el) { return el; });
      }

      function matchesRange(value, checks, attrMin, attrMax) {
        if (checks.length === 0) return true;
        return checks.some(function (el) {
          var min = Number(el.getAttribute(attrMin));
          var max = Number(el.getAttribute(attrMax));
          return value >= min && value <= max;
        });
      }

      function sortCards(list) {
        var sort = sortSelect.value;
        return list.sort(function (a, b) {
          if (sort === 'default') return Number(a.dataset.id) - Number(b.dataset.id);
          if (sort === 'price-asc') return Number(a.dataset.price) - Number(b.dataset.price);
          if (sort === 'price-desc') return Number(b.dataset.price) - Number(a.dataset.price);
          if (sort === 'name') return a.dataset.name.localeCompare(b.dataset.name);
          return Number(a.dataset.id) - Number(b.dataset.id);
        });
      }

      function renderPageNumbers(totalPages) {
        pageNumbers.innerHTML = '';
        var maxVisible = 5;
        var start = Math.max(1, currentPage - 2);
        var end = Math.min(totalPages, start + maxVisible - 1);
        if (end - start < maxVisible - 1) start = Math.max(1, end - maxVisible + 1);

        for (var i = start; i <= end; i++) {
          var btn = document.createElement('button');
          btn.type = 'button';
          btn.textContent = String(i);
          btn.className = 'mx-0.5 flex h-9 w-9 items-center justify-center rounded-full text-sm font-semibold ' +
            (i === currentPage ? 'bg-blue-700 text-white' : 'text-slate-600 hover:bg-stone-100');
          btn.addEventListener('click', (function (page) {
            return function () { currentPage = page; renderPage(); grid.scrollIntoView({ behavior: 'smooth', block: 'start' }); };
          })(i));
          pageNumbers.appendChild(btn);
        }
      }

      function renderPage() {
        var totalPages = Math.max(1, Math.ceil(filteredCards.length / PAGE_SIZE));
        if (currentPage > totalPages) currentPage = totalPages;
        if (currentPage < 1) currentPage = 1;

        var start = (currentPage - 1) * PAGE_SIZE;
        var end = start + PAGE_SIZE;
        var pageCards = filteredCards.slice(start, end);

        filteredCards.forEach(function (card) { grid.appendChild(card); });

        cards.forEach(function (card) {
          var inFilter = filteredCards.includes(card);
          var onPage = pageCards.includes(card);
          card.classList.toggle('hidden', !inFilter || !onPage);
        });

        var total = filteredCards.length;
        resultCount.textContent = total === 0 ? 'Showing 0 items' : 'Showing ' + (start + 1) + '–' + Math.min(end, total) + ' of ' + total + ' items';

        prevPageBtn.disabled = currentPage <= 1;
        nextPageBtn.disabled = currentPage >= totalPages;
        renderPageNumbers(totalPages);

        var hasResults = total > 0;
        emptyState.classList.toggle('hidden', hasResults);
        grid.classList.toggle('hidden', !hasResults);
        pagination.classList.toggle('hidden', !hasResults || totalPages <= 1);
      }

      function applyFilters() {
        var query = searchInput.value.trim().toLowerCase();
        var categories = getCheckedValues('.filter-category').map(function (el) { return el.value; });
        var conditions = getCheckedValues('.filter-condition').map(function (el) { return el.value; });
        var yearChecks = getCheckedValues('.filter-year');
        var priceChecks = getCheckedValues('.filter-price');

        filteredCards = cards.filter(function (card) {
          if (query && !card.dataset.name.includes(query)) return false;
          if (categories.length && categories.indexOf(card.dataset.category) === -1) return false;
          if (conditions.length && conditions.indexOf(card.dataset.condition) === -1) return false;
          if (!matchesRange(Number(card.dataset.year), yearChecks, 'data-min', 'data-max')) return false;
          if (!matchesRange(Number(card.dataset.price), priceChecks, 'data-min', 'data-max')) return false;
          return true;
        });

        sortCards(filteredCards);
        currentPage = 1;
        renderPage();
      }

      document.querySelectorAll('.filter-category, .filter-condition, .filter-year, .filter-price').forEach(function (el) {
        el.addEventListener('change', applyFilters);
      });
      searchInput.addEventListener('input', applyFilters);
      sortSelect.addEventListener('change', applyFilters);

      clearBtn.addEventListener('click', function () {
        document.querySelectorAll('.filter-category, .filter-condition, .filter-year, .filter-price').forEach(function (el) { el.checked = false; });
        searchInput.value = '';
        applyFilters();
      });

      prevPageBtn.addEventListener('click', function () {
        if (currentPage > 1) { currentPage--; renderPage(); grid.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
      });
      nextPageBtn.addEventListener('click', function () {
        var totalPages = Math.ceil(filteredCards.length / PAGE_SIZE);
        if (currentPage < totalPages) { currentPage++; renderPage(); grid.scrollIntoView({ behavior: 'smooth', block: 'start' }); }
      });

      if (window.location.hash) {
        var hash = window.location.hash.replace('#category-', '').replace(/-/g, ' ');
        document.querySelectorAll('.filter-category').forEach(function (el) {
          if (el.value.toLowerCase() === hash || el.value.toLowerCase().replace('!', '') === hash.replace('!', '')) {
            el.checked = true;
          }
        });
      }

      applyFilters();
    })();
  </script>
}
