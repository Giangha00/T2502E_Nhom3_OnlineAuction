@model int

@if (Model > 0)
{
    <span class="ml-auto inline-flex min-w-[1.25rem] items-center justify-center rounded-full bg-warning-500 px-1.5 py-0.5 text-[10px] font-bold text-white">
        @(Model > 99 ? "99+" : Model.ToString())
    </span>
}
