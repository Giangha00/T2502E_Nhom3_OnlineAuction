@model OnlineAuction.Models.AuctionItemViewModel
@using OnlineAuction.Entities
@{
    var isBuyNow = string.Equals(Model.ListingType, ListingTypes.BuyNow, StringComparison.OrdinalIgnoreCase);
}

<partial name="~/Views/Shared/Partials/_TradingCardAuctionCard.cshtml"
         model="@(new AuctionCardViewModel
         {
             Item = Model,
             Variant = "home",
             CardMode = isBuyNow ? "buynow" : "auction",
             ShowBidLink = true,
             ShowWatchlistButton = false,
             ShowTimeRemaining = !isBuyNow
         })"/>
